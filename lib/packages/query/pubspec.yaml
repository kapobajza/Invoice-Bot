name: query
description: "A package to create queries and cache them, inspired by Tanstack Query"
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.5.4

dev_dependencies:
  test: ^1.25.8
  mockito: ^5.4.4
  build_runner: ^2.4.13
